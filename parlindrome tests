#_*_ utf-8 _*_
'''parlindrome test: to test input is a parlindrome or not. BTW prlindrome number should be larger than 100 '''
def is_parlindrome(n):
##    print(n)
    str_n = str(n)
    if str_n.isdecimal():
        pass
    else:
        print('input is not a decimal number, please restart and try again ')
        return
    list_n = list(str_n)
    list_n.reverse()
    num_n = map(str, list_n)
    num_n = ''.join(num_n)
    num_n = int(num_n)
    if num_n == n and n >100:
        return num_n
    else:
        pass
